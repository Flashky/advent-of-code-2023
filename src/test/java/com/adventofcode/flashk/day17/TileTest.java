package com.adventofcode.flashk.day17;

import com.adventofcode.flashk.common.Vector2;
import org.junit.jupiter.api.Test;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class TileTest {

    /*
    @Test
    void nextDirectionsRight() {
        Tile tile = new Tile(50, 2,5);

        List<Vector2> directions = tile.nextDirections(Vector2.right(),1);
        assertEquals(3, directions.size());

        directions = tile.nextDirections(Vector2.right(),3);
        assertEquals(2, directions.size());

        directions = tile.nextDirections(Vector2.right(),4);
        assertEquals(2, directions.size());

    }

    @Test
    void nextDirectionsLeft() {
        Tile tile = new Tile(50, 2,5);

        List<Vector2> directions = tile.nextDirections(Vector2.left(),1);
        assertEquals(3, directions.size());

        directions = tile.nextDirections(Vector2.left(),3);
        assertEquals(2, directions.size());

        directions = tile.nextDirections(Vector2.left(),4);
        assertEquals(2, directions.size());

    }

    @Test
    void nextDirectionsUp() {
        Tile tile = new Tile(50, 2,5);

        List<Vector2> directions = tile.nextDirections(Vector2.down(),1);
        assertEquals(3, directions.size());

        directions = tile.nextDirections(Vector2.left(),3);
        assertEquals(2, directions.size());

        directions = tile.nextDirections(Vector2.left(),4);
        assertEquals(2, directions.size());

    }

    @Test
    void nextDirectionsDown() {
        Tile tile = new Tile(50, 2,5);

        List<Vector2> directions = tile.nextDirections(Vector2.up(),1);
        assertEquals(3, directions.size());

        directions = tile.nextDirections(Vector2.left(),3);
        assertEquals(2, directions.size());

        directions = tile.nextDirections(Vector2.left(),4);
        assertEquals(2, directions.size());

    }
*/
}